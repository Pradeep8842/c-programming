#include<stdio.h>
int main()
{
float a,b,area;
printf("Give value of rectangle length :");
scanf("%f",&a);
printf("Give value of rectangle wide :");
scanf("%f",&b);
area=a*b;
printf("Area of rectangle :%.2f",area);
return 0;
}
